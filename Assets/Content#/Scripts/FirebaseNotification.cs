using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Firebase.Messaging;
using System;

public class FirebaseNotification : MonoBehaviour
{
    // Start is called before the first frame update
    //void Start()
    //{
    //    Firebase.Messaging.FirebaseMessaging.TokenReceived += OnTokenReceived;
    //    Firebase.Messaging.FirebaseMessaging.MessageReceived += OnMessageReceived;
    //}

    //public void OnTokenReceived (object sender, FirebaseMessaging. TokenReceivedEventArgs token)
    //{
    //    Debug.Log("recieved reg token" + token.Token);
    //}

    //public void OnMessageReceived (object sender, FirebaseMessaging.MessageReceivedEventArgs e) 
    //{
    //    Debug.Log("recieved reg token" + e.Message.From);
    //}

   
}
